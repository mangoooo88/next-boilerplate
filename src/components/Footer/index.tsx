import styled from "styled-components";

const FooterWrapper = styled.footer`
  margin-top: auto;
  text-align: center;
`

function Footer() {
  return (
    <FooterWrapper>
      푸터
    </FooterWrapper>
  );
}

export default Footer;